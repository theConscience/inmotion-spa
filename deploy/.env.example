# --- Telegram / CRM / Admin ---
TG_BOT_TOKEN=
WEBHOOK_SECRET=
MOYKLASS_API_KEY=
ADMIN_TOKEN=dev-admin-token

# --- Triggers/Rules (плейсхолдеры) ---
DAYS_TO_END=5
REMAIN_VISITS=1
FREEZE_END_DAYS=3

# --- Postgres ---
POSTGRES_DB=inmotion
POSTGRES_USER=inmotion
POSTGRES_PASSWORD=change_me
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# --- Ports (внутри контейнеров) ---
PORT_API=3000
PORT_ADMIN=4000

# --- Domains/HTTPS (позже) ---
LE_EMAIL=
DOMAIN_API=
DOMAIN_ADMIN=
ADMIN_USER=
ADMIN_PASS=
